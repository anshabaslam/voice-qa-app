
GROQ_API_KEY=
USE_GROQ=true

# Option 2: Ollama (FREE - Local AI) (optional) 
# ollama is a fallback for grok. if you need to use install ollama locally
USE_OLLAMA=true
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Text-to-Speech Service
ELEVENLABS_API_KEY=

# Application Configuration
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000
ENVIRONMENT=development

# Optional: Redis for session caching (uses in-memory if not available)
# its not manadatory but keep this variable in .env also
USE_REDIS=false
REDIS_URL=redis://localhost:6379